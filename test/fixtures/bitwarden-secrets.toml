# Test fixture: workstation with the Bitwarden backend enabled.
#
# Exists purely so the secrets-bearing templates are render-checked. The `bw`
# CLI is never invoked during a render — templates that call it are excluded by
# .chezmoiignore and test/render.sh honours that exclusion.

[data]
    # Identity is NOT here: it lives in home/.chezmoidata/identity.toml, which
    # is repo data rather than a per-machine answer. Fixtures carry only what
    # `chezmoi init` actually asks.
    profile        = "workstation"
    tier           = "full"
    tierRank       = 2

    secretsBackend = "bitwarden"
    gpgSign        = true
    signingKey     = "~/.ssh/id_ed25519.pub"

    fonts          = true
    terminals      = true
    autoAttach     = true
    chsh           = true
    staleness      = true
    nodeHeapMB     = 8192
