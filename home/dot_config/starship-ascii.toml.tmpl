{{- /* ASCII fallback, auto-selected on a bare TTY with no Nerd Font
       (report.md §8.6). Same layout, different glyphs — see the partial. */ -}}
{{- template "starship" (dict "glyphs" "ascii" "theme" .theme) -}}
